# Core dependencies
fastapi
uvicorn
aiohttp
python-multipart

# LNbits integration
requests
pylnbits
cryptography
python-dotenv

# Language services
deep-translator
beautifulsoup4
ddgs
# openai-whisper and ffmpeg-python excluded from cloud deploy (pulls PyTorch ~2GB)
# Install locally if you need transcription: pip install openai-whisper ffmpeg-python

# Nostr discovery (via GitHub)
git+https://github.com/jeffthibault/python-nostr.git

# Enhanced security and monitoring
PyJWT
psutil
jsonschema
pydantic

# Testing utilities
httpx
pytest
pytest-asyncio
pytest-mock

# Security dependencies (cryptography already listed above; secrets/hashlib are stdlib)
